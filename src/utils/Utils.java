package utils;

public class Utils {
    public static int randomInt(int low, int high){
        return(int) Math.floor(Math.random() * (high - low + 1)
        + low);
    }

//    public static String prettyP
}
